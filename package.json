{
  "name": "vscode-webview-rpc-lib",
  "version": "0.0.1",
  "description": "An RPC library for VSCode WebViews",
  "main": "index.js",
  "scripts": {
    "compile": "rimraf ./out.ext && tsc -p ./tsconfig.ext.json",
    "watch": "tsc -w -p ./tsconfig.ext.json",
    "compile-browser": "rimraf ./out.browser && tsc -p tsconfig.browser.json",
    "watch-browser": "tsc -w -p ./tsconfig.browser.json",
    "lint": "eslint ./**/*.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/SAP/vscode-webview-rpc-lib"
  },
  "author": "Ido Perez",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^10.12.21",
    "@types/vscode": "^1.38.0",
    "@types/ws": "^6.0.3",
    "@typescript-eslint/eslint-plugin": "^2.6.1",
    "@typescript-eslint/parser": "^2.6.1",
    "eslint": "^6.6.0",
    "rimraf": "^3.0.0",
    "typescript": "^3.6.3"
  },
  "dependencies": {
    "ws": "^7.1.2"
  }
}
